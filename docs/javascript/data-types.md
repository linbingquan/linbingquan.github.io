# javascript 数据类型

原始数据类型（基本类型）：

- 字符串（String）
- 数字(Number)
- 布尔(Boolean)
- 空（Null）
- 未定义（Undefined）
- Symbol

> Symbol 是 ES6 引入了一种新的原始数据类型，表示独一无二的值

引用数据类型（对象类型）：

- 对象(Object)
- 数组(Array)
- 函数(Function)

还有两个特殊的对象：

- 正则（RegExp）
- 日期（Date）

