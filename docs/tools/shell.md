
# Shell脚本

#### npm查看安装过的模块或包
```shell
npm ls --depth 0
```
[npm docs](https://docs.npmjs.com/cli/ls)

#### cmd alias
```shell
@doskey hs=http-server ./ $*
```

#### adb tool
```shell
adb reverse tcp:8080 tcp:8080
```